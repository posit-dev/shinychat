% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/chat_history.R
\name{history_options}
\alias{history_options}
\title{Configure chat history options}
\usage{
history_options(
  restore_mode = c("browser", "url", "none", "bookmark"),
  store = "auto",
  scope = NULL,
  title = "auto",
  max_store_mb = 100
)
}
\arguments{
\item{restore_mode}{How a previous conversation is reloaded when the page
opens. \code{"browser"} (the default) stores the active conversation ID in
\code{localStorage} so it survives page reloads. \code{"url"} stores the ID as a
plain \verb{?shinychat_conversation_id=<id>} query parameter so the active
conversation is visible in the address bar and users can share or bookmark
specific conversations; no server bookmarking configuration is required.
\code{"bookmark"} participates in Shiny server bookmarking: after every LLM
response a fresh server bookmark is minted and the address bar updates to
\verb{?_state_id_=...}. Requires \code{bookmarkStore = "server"} in the Shiny app
options. On in-session conversation switches, navigates to the target
conversation's bookmark URL if one exists. Note that \code{on_save} values are
captured and stored in the conversation record in this mode, but \code{on_restore}
never fires — use \code{session$onRestore()} to read them back if needed.
\code{"none"} disables automatic restore entirely.}

\item{store}{Storage backend: \code{"auto"} (default: memory in dev, file in
production), \code{"memory"}, \code{"file"}, or a \link{ConversationStore} R6 instance.}

\item{scope}{Storage namespace for conversations. A string, a
\verb{function(session)} returning a string, or \code{NULL} (default: uses
\code{session$user} if authenticated, otherwise a per-browser token).
Pass a shared string to allow multiple users to share history — for
example \code{session$groups[[1]]} to scope by group, or a constant like
\code{"global"} to share across all users.}

\item{title}{Title generation strategy. \code{"auto"} (default) for LLM-generated
titles, a \verb{function(recorded_turns)} for custom titles, or \code{NULL} to skip
LLM titling (the conversation keeps its initial timestamp-based name).}

\item{max_store_mb}{Maximum total storage in megabytes per scope. Oldest
conversations are evicted when the limit is exceeded. Defaults to \code{100}.}
}
\value{
A configuration object for use with \code{\link[=chat_enable_history]{chat_enable_history()}}.
}
\description{
Configure chat history options
}
