% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/chat.R
\name{chat_greeting}
\alias{chat_greeting}
\title{Create a greeting for a chat UI}
\usage{
chat_greeting(
  content,
  dismissible = TRUE,
  as_assistant_message = FALSE,
  include_in_history = FALSE
)
}
\arguments{
\item{content}{The greeting content. Can be:
\itemize{
\item A string, interpreted as markdown.
\item An \code{\link[htmltools:HTML]{htmltools::HTML()}} object, rendered as raw HTML.
\item An htmltools tag or \code{\link[htmltools:tagList]{htmltools::tagList()}}, including Shiny
inputs/outputs.
\item A generator or promise (only valid when used with \code{chat_set_greeting()}).
}}

\item{dismissible}{Whether the greeting is automatically dismissed when the
user sends a message. Defaults to \code{TRUE}. Cannot be \code{TRUE} when
\code{as_assistant_message} is also \code{TRUE}.}

\item{as_assistant_message}{Whether to display the greeting as a regular
assistant message instead of a stand-alone greeting. Defaults to \code{FALSE}.}

\item{include_in_history}{Whether to include the greeting content in the
model's conversation history. This is a server-side option only and does
not affect the client-side rendering. Defaults to \code{FALSE}.}
}
\value{
An S3 object of class \code{"chat_greeting"}.
}
\description{
Creates a greeting object for use with \code{\link[=chat_ui]{chat_ui()}} or \code{chat_set_greeting()}.
A greeting is displayed when the chat first loads and is dismissed when the
user sends their first message.
}
\examples{
\dontshow{if (interactive()) withAutoprint(\{ # examplesIf}
library(shiny)
library(bslib)
library(shinychat)

ui <- page_fillable(
  chat_ui(
    "chat",
    greeting = chat_greeting("## Welcome!\n\nHow can I help you today?")
  )
)

server <- function(input, output, session) {
  observeEvent(input$chat_user_input, {
    response <- paste0("You said: ", input$chat_user_input)
    chat_append("chat", response)
  })
}

shinyApp(ui, server)
\dontshow{\}) # examplesIf}
}
